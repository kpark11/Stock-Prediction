This program is developed to predict stock prices.

The packages include tensorflow, sklearn, yahoo_fin, collection, os, numpy, pandas, and random. 

The sequential model is used with recurrent neural network (LSTM).


